[project]
name = "taipei-elearn-assistant"
version = "0.1.1"
description = "臺北 e 大學習輔助程式"
requires-python = ">=3.12"
dependencies = ["PySide6>=6.8,<7", "playwright>=1.50,<2"]

[project.optional-dependencies]
test = ["pytest>=8.3", "pytest-qt>=4.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
